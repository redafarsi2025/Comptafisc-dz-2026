
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user profile within the ComptaFisc-DZ application, capable of having various roles including SaaS administrator, dossier manager, or accountant. This entity stores non-authentication-sensitive user data and role assignments.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "email": {
          "type": "string",
          "description": "The user's email address, serving as a primary contact and a unique identifier in the authentication system.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the user."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the user."
        },
        "roles": {
          "type": "array",
          "description": "A list of roles assigned to the user, such as 'saas_admin', 'dossier_manager', or 'comptable'.",
          "items": {
            "type": "string"
          }
        },
        "tenantIds": {
          "type": "array",
          "description": "References to Tenant entities this user has access to. (Relationship: Tenant N:N UserProfile)",
          "items": {
            "type": "string"
          }
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the user profile was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "firstName",
        "lastName",
        "roles",
        "createdAt"
      ]
    },
    "Tenant": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Tenant",
      "type": "object",
      "description": "Represents an independent accounting client or company utilizing the ComptaFisc-DZ platform in a multi-tenant setup. SaaS administrators manage these tenants.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Tenant entity."
        },
        "name": {
          "type": "string",
          "description": "The official registered name of the company or accounting client."
        },
        "taxIdNumber": {
          "type": "string",
          "description": "The unique tax identification number (e.g., NIF in Algeria) of the tenant."
        },
        "tradeRegisterNumber": {
          "type": "string",
          "description": "The trade register number (e.g., RC in Algeria) of the tenant."
        },
        "address": {
          "type": "string",
          "description": "The physical registered address of the tenant."
        },
        "contactEmail": {
          "type": "string",
          "description": "The primary contact email address for the tenant.",
          "format": "email"
        },
        "phoneNumber": {
          "type": "string",
          "description": "The primary contact phone number for the tenant."
        },
        "isActive": {
          "type": "boolean",
          "description": "Indicates whether the tenant account is currently active on the platform, manageable by SaaS administrators."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the tenant account was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the tenant account was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "taxIdNumber",
        "address",
        "contactEmail",
        "isActive",
        "createdAt"
      ]
    },
    "SubscriptionPlan": {
      "title": "SubscriptionPlan",
      "type": "object",
      "description": "Defines a commercial plan available for users. Plans bundle specific modules and impose limits.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "price": { "type": "number" },
        "period": { "type": "string", "enum": ["DA/mois", "DA/an"] },
        "description": { "type": "string" },
        "isActive": { "type": "boolean" },
        "modules": {
          "type": "object",
          "description": "Mapping of functional modules to their status in this plan."
        },
        "limits": {
          "type": "object",
          "properties": {
            "invoices": { "type": "string" },
            "users": { "type": "string" },
            "companies": { "type": "string" },
            "storage": { "type": "string" },
            "support": { "type": "string" }
          }
        }
      },
      "required": ["id", "name", "price", "isActive"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/userProfiles/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores individual user profile data.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
      {
        "path": "/saas_admins/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "A dedicated collection to flag users as SaaS administrators.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the SaaS administrator."
            }
          ]
        }
      },
      {
        "path": "/plans/{planId}",
        "definition": {
          "entityName": "SubscriptionPlan",
          "schema": {
            "$ref": "#/backend/entities/SubscriptionPlan"
          },
          "description": "Stores commercial subscription plans. Managed by SaaS Admins.",
          "params": [
            {
              "name": "planId",
              "description": "The unique ID of the plan."
            }
          ]
        }
      }
    ]
  }
}
